<%= form_tag({controller: "main", action: "finder"}, method: "post") do %>
  <input name="fname" placeholder="first name">
  <input name="lname" placeholder="last name">
  <input name="url" placeholder="url (ex: wepay.com)">
  <%= submit_tag %>
<% end %>
